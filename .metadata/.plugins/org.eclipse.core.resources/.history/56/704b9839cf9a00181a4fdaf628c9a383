create table SINGER (SINGER_ID INTEGER NOT NULL GENERATED ALWAYS AS IDENTITY (START WITH 1, INCREMENT BY 1), FIRST_NAME VARCHAR(60), LAST_NAME VARCHAR(40), BIRTH_DAY DATE, VERSION INT, EMAIL VARCHAR(30), , PRIMARY KEY (SINGER_ID));
